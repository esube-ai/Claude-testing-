PORT=3001
NODE_ENV=development
DB_PATH=./data/str.db
SCRAPE_ON_STARTUP=true
ALLOWED_ORIGIN=http://localhost:5173

# Vercel automatically sets VERCEL=1, which switches to in-memory SQLite.
# No environment variables needed in the Vercel dashboard for basic operation.
